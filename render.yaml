services:
  - type: web
    name: gym-management-system
    env: docker
    dockerfilePath: ./Dockerfile
    preDeployCommand: python init_db.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
