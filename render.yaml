services:
  - type: web
    name: gym-management-system
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gym-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: gym-db
    plan: free
